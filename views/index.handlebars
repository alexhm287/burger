<h1>Burgers!</h1>

<h2>Undevoured Burgers!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      <li>
        {{burger_name}}
        <button class="devour-burger" data-id="{{id}}">
          Devour
        </button>
      </li>
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that you've devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      <li>
        {{burger_name}}
        <button class="devour-burger" data-id="{{id}}">
          Devour
        </button>
      </li>
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">
  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="burger_name" name="name">
  </div>
  <button type="submit">Add Burger</button>
</form>
